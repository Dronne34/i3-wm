# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
set $mod Mod4
focus_on_window_activation focus  
focus_follows_mouse yes
hide_edge_borders none
# focus_follows_mouse no
#default_floating_border none
gaps outer 10
gaps inner 10
# gaps top 2
# gaps bottom 2

workspace_auto_back_and_forth no
show_marks yes
#force_display_urgency_hint 100 ms
#default_border pixel 2
#smart_gaps on
#border_radius 5
#smart_gaps inverse_outer
#hide_edge_borders smart_no_gaps
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart

# exec --no-startup-id dex --autostart --environment i3
# exec --no-startup-id ~/.screenlayout/lg.sh
# exec --no-startup-id /tmp/eqcommands.sh
# exec --no-startup-id start-pulseaudio-x11
# exec --no-startup-id /bin/start-engine --client-console
# exec --no-startup-id iwctl device wlan1 set-property Powered off
# exec --no-startup-id iwctl device wlan0 set-property Powered off
# exec --no-startup-id feh --no-fehbg --bg-scale $HOME/.config/screen/Desktop-60.jpg
# exec --no-startup-id nitrogen --restore
# exec --no-startup-id picom -bCG
# exec --no-startup-id sxhkd
# exec --no-startup-id xrdb -merge ~/.Xresources
# exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# exec --no-startup-id playerctld daemon
# exec --no-startup-id polybar
# exec --no-startup-id conky -c ~/.config/conky/clock.conf
# exec --no-startup-id xss-lock  --transfer-sleep-lock -- ~/.config/i3/scripts/lock --nofork
# exec --no-startup-id mpd
# exec --no-startup-id nm-applet
# exec_always --no-startup-id autotiling
# exec_always --no-startup-id blueman-applet
exec_always --no-startup-id python3 ~/.local/bin/autolayout.py

# Key
bindsym $mod+d       exec --no-startup-id dmenu_run
bindsym Print        exec --no-startup-id  maim ~/Pictures/$(date +%s).png; exec --no-startup-id notify-send "Screenshot done" -i simplescreenrecorder-idle
bindsym $mod+b border toggle
# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status | dunstify  -r 3456 -t 700 -i audio-volume-high   "volume: $(pamixer --get-volume)%" | canberra-gtk-play -i audio-volume-change -d "changeVolume" 
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status | dunstify  -r 3456 -t 700 -i audio-volume-medium "volume: $(pamixer --get-volume)%" | canberra-gtk-play -i audio-volume-change -d "changeVolume"
# bindsym XF86AudioMute exec        --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status       | dunstify        -r 3456 -t 700 -i audio-volume-muted  "mute: $(pamixer --get-mute)" | canberra-gtk-play -i audio-volume-change -d "changeVolume"
# bindsym XF86AudioMicMute exec     --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status | dunstify  -r 3456 -t 700 -i audio-volume-muted  "mute: $(pamixer --get-mute)" | canberra-gtk-play -i audio-volume-change -d "changeVolume"

# Use xbacklight
bindsym XF86MonBrightnessUp   exec --no-startup-id xbacklight -inc 10 # increase screen brightness
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 10 # decrease screen brightness

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Orientation
default_orientation auto

# start a terminal
bindsym $mod+Return exec --no-startup-id alacritty 


# kill focused window
bindsym $mod+q kill

# Gaps
bindsym $mod+KP_Add gaps inner current plus 5
bindsym $mod+KP_Subtract gaps inner current minus 5
bindsym $mod+Shift+KP_Add gaps inner all plus 5
bindsym $mod+Shift+KP_Subtract gaps inner all minus 5

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Change tilling direction
bindsym $mod+h split h;exec --no-startup-id notify-send 'Tile horizontally'
bindsym $mod+v split v;exec --no-startup-id notify-send 'Tile vertically'
bindsym $mod+Shift+q split toggle;;exec --no-startup-id notify-send 'Splitting toggle'

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# Change container layout
bindsym $mod+s layout stacking;exec --no-startup-id notify-send 'Stacking mode'
bindsym $mod+w layout tabbed;exec --no-startup-id notify-send 'Tabbing mode'
bindsym $mod+e layout toggle split;exec --no-startup-id notify-send 'Splitting mode'

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle
  
# focus the parent container
bindsym $mod+a focus parent

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle


# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"


# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8

# Move focused container to workspace
bindsym $mod+Ctrl+1 move container to workspace $ws1
bindsym $mod+Ctrl+2 move container to workspace $ws2
bindsym $mod+Ctrl+3 move container to workspace $ws3
bindsym $mod+Ctrl+4 move container to workspace $ws4
bindsym $mod+Ctrl+5 move container to workspace $ws5
bindsym $mod+Ctrl+6 move container to workspace $ws6
bindsym $mod+Ctrl+7 move container to workspace $ws7
bindsym $mod+Ctrl+8 move container to workspace $ws8

# You can also use any non-zero value if you'd like to have a border
for_window [class=".*"]                   border pixel 1
for_window [class="vlc-main"]             border pixel 0
for_window [class="vlc"]                  border pixel 1
for_window [title="Open File"]            floating enable;move absolute position
for_window [class="Nitrogen"]             floating enable;move absolute position center
for_window [title="^Media viewer"]        floating enable;move absolute position center
for_window [class="Gnome-system-monitor"] floating enable;move absolute position center
for_window [class="File-roller"]          floating enable;move absolute position center
for_window [class="Gpick"]                floating enable;move absolute position center

# Correct handling of floating windows
for_window [window_role="pop-up"]         floating enable
for_window [window_role="bubble"]         floating enable
for_window [window_role="task_dialog"]    floating enable
for_window [window_role="dialog"]         floating enable
for_window [window_role="menu"]           floating enable
for_window [window_role="Preferences"]    floating enable


# Open specific applications in floating mode
#for_window [class="Chromium"]  move container to workspace $ws3; workspace $ws3
for_window [class="Brave-browser"]        move container to workspace $ws3; workspace $ws3;
for_window [class="Subl"]                 move container to workspace $ws1; workspace $ws1;
for_window [class="Alacritty"]            move container to workspace $ws1; workspace $ws1;
for_window [class="Pcmanfm"]              move container to workspace $ws1; workspace $ws1;
for_window [class="Subl"]                 move absolute position center

for_window [class="st-256color"]          move container to workspace $ws5; workspace $ws5
for_window [class="TelegramDesktop"]      move container to workspace $ws4; workspace $ws4
for_window [class="Hexchat"]              move container to workspace $ws4; workspace $ws4
for_window [class="code-oss"]             move container to workspace $ws1; workspace $ws1
for_window [class="mpv"]                  move container to workspace $ws5; workspace $ws5

#for_window [class="URxvt"] floating enable, move position 10 px 30 px
#for_window [class="URxvt"] floating enabled position left
for_window [title="File Operation Progress"]                  floating enable
floating_minimum_size 75 x 50
floating_maximum_size 1024 x 762


bindsym $mod+0 mode "$mode_system"
set $mode_system (l)ock, (e)xit, switch_(u)ser, (s)uspend, (h)ibernate, (r)eboot, (Shift+s)hutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym s exec --no-startup-id i3exit suspend, mode "default"
    bindsym u exec --no-startup-id i3exit switch_user, mode "default"
    bindsym e exec --no-startup-id i3exit logout, mode "default"
    bindsym h exec --no-startup-id i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id i3exit shutdown, mode "default"

    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}


# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# Resizing by 5
# resize window (you can also use the mouse for that)
bindsym $mod+Ctrl+Right resize shrink width 5 px or 5 ppt
bindsym $mod+Ctrl+Up    resize grow height 5 px or 5 ppt
bindsym $mod+Ctrl+Down  resize shrink height 5 px or 5 ppt
bindsym $mod+Ctrl+Left  resize grow width 5 px or 5 ppt

# no_focus [class="Brave-browser"]
# no_focus [class="mpv"]

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status-rs ~/.config/i3status-rust/config.toml
        #status_command i3status
        font pango:DejaVu Sans Mono, FontAwesome 10
        #tray_output HDMI-1
        output primary
        output eDP1
        binding_mode_indicator yes
        tray_padding 2
        separator_symbol ":|:"
        workspace_buttons yes
        workspace_min_width 25
        strip_workspace_name no
        strip_workspace_numbers yes
        position top
        #separator_symbol ""

colors {
        separator #666666
        background #222222
        statusline #dddddd
        focused_workspace #0088CC #0088CC #ffffff
        active_workspace #333333 #333333 #ffffff
        inactive_workspace #333333 #333333 #ffffff
        urgent_workspace #2f343a #900000 #ffffff
    }
}
# Theme colors
# class                   border  backgr. text    indic.   child_border
client.focused          #CF2FAF #556064 #80FFF9 #CF2FAF    #CF2FAF
client.focused_inactive #1ABC9C #2F3D44 #1ABC9C #1ABC9C    #1ABC9C
client.unfocused        #2F3D44 #2F3D44 #1ABC9C #E5201D    #FFFF7F
client.urgent           #CB4B16 #FDF6E3 #1ABC9C #268BD2    #AA55FF
client.placeholder      #000000 #0c0c0c #ffffff #000000 
client.background       #2B2C2B
